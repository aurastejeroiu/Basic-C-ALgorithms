#include <iostream>

using namespace std;
void Afis(long n)   ///{număr natural}
{
    long p,i;
    i=p=1;
    while ( i<n && p!=)
    {
        i+=1;
        x=p*i;
        p=x%10;
    }
    cout<<p<<' '<<i<<endl;
}
int main()
{   Afis(-15);
    Afis(0);
    Afis(1);
    Afis(6);
    Afis(20);
    Afis(100);
    cout << "Hello world!" << endl;
    return 0;
}
